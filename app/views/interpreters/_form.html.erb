<%= form_for(@interpreter) do |f| %>
  <% if @interpreter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interpreter.errors.count, "error") %> prohibited this interpreter from being saved:</h2>last_
      <ul>
      <% @interpreter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_level %> (H,M,L)<br>
    <%= f.text_field :skill_level %>
  </div>
  <div class="field">
    <%= f.label :hourly_rate %> (cents)<br>
    <%= f.text_field :hourly_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
