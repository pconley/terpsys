<h1>Edit Customer</h1>

<%= simple_form_for(@customer, :url => { :controller => "customers", :action => "update" },) do |f| %>
  <%= render 'form', f: f, resource: @customer %>
<% end %>

<%= link_to 'Show Customer Details', admin_customer_path(@customer) %> |
<%= link_to 'Go to Customers', admin_customers_path %>

