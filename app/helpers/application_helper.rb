module ApplicationHelper
      
  # def dash_path_for(user)
  #   return interpreter_dash_path if user.try(:interpreter?)
  #   return consumer_dash_path    if user.try(:consumer?)
  #   return admin_dashboard_path  if user.try(:admin?)
  #   root_path
  # end
  
end
